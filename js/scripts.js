document.addEventListener('DOMContentLoaded',function(){var yearEl=document.getElementById('year');if(yearEl)yearEl.textContent=String(new Date().getFullYear());const canvas=document.getElementById('matrix');const ctx=canvas?canvas.getContext('2d'):null;const fontFamily='Share Tech Mono';const fontSize=16;const glyphBase=0x30a0;let cssW=0,cssH=0,dpr=1,cols=0,yPos=[];let rafId=null;let color='#00ff9c';function sizeCanvas(){if(!canvas||!ctx)return;dpr=window.devicePixelRatio||1;cssW=window.innerWidth;cssH=Math.max(window.innerHeight,680);canvas.width=Math.floor(cssW*dpr);canvas.height=Math.floor(cssH*dpr);canvas.style.width=cssW+'px';canvas.style.height=cssH+'px';ctx.setTransform(dpr,0,0,dpr,0,0);ctx.font=fontSize+'px '+fontFamily;cols=Math.ceil(cssW/fontSize);yPos=new Array(cols).fill(0)}
function frame(){if(!ctx)return;ctx.fillStyle='rgba(11,14,15,0.10)';ctx.fillRect(0,0,cssW,cssH);ctx.fillStyle=color;for(let i=0;i<cols;i++){const ch=String.fromCharCode(glyphBase+Math.random()*96);const x=i*fontSize;ctx.fillText(ch,x,yPos[i]);yPos[i]=(yPos[i]>cssH+Math.random()*10000)?0:(yPos[i]+fontSize)}
rafId=requestAnimationFrame(frame)}
function start(){if(!canvas||!ctx)return;sizeCanvas();if(rafId)cancelAnimationFrame(rafId);rafId=requestAnimationFrame(frame)}
window.addEventListener('resize',start);function afterFontsReady(cb){if(document.fonts&&document.fonts.ready){document.fonts.ready.then(cb).catch(cb)}else{setTimeout(cb,50)}}
afterFontsReady(start);window.handleContact=function(e){e.preventDefault();const data=new FormData(e.target);const nome=encodeURIComponent(data.get('nome')||'');const email=encodeURIComponent(data.get('email')||'');const msg=encodeURIComponent(data.get('mensagem')||'');const subject='Contato via site — '+nome;const body='De: '+nome+'%0AEmail: '+email+'%0A%0A'+msg;window.location.href='mailto:thematrixbunny@proton.me?subject='+subject+'&body='+body}})